apiVersion: v1
kind: Service
metadata:
  labels:
    app: sampleapp
  name: sampleapp
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8089
    nodePort: 30876
  selector:
    app: sampleapp
  type: NodePort
